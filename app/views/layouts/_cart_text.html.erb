<!--<span class="glyphicon glyphicon-shopping-cart" aria-hidden="true"></span>-->
<%= link_to "#{current_order.order_items.size} Items in Cart ( #{number_to_currency current_order.subtotal} )", cart_path, class: "btn btn-large btn-primary" , style: "color:white"%>